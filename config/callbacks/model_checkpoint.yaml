model_checkpoint:
  _target_: lightning.pytorch.callbacks.ModelCheckpoint
  dirpath: ${paths.run_dir}/checkpoints
  monitor: val_loss
  mode: min
  save_last: true
  filename: epoch={epoch:06d}_validate-loss={val_loss:.6f}
  auto_insert_metric_name: false
