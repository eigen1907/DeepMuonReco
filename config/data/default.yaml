_target_: deepmuonreco.data.DataModule

dataset: deepmuonreco.data.InnerTrackSelectionDataset

root: ${paths.data_dir}

train_file: train.h5
val_file: val.h5
test_file: val.h5
predict_file: null

track_feature_list: ${exp.track_feature_list}
segment_feature_list: ${exp.segment_feature_list}
hit_feature_list: ${exp.hit_feature_list}

batch_size: 32
eval_batch_size: 32
train_sampler: null

num_workers: 0
pin_memory: false

train_max_events: null
val_max_events: null
test_max_events: null
predict_max_events: null
